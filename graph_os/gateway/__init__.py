"""REST gateway — RF-ADR-009 §2 Phase 5 target.

Owns the `/api/...` REST surface kept in lockstep with the MCP tool surface
(agent-utilities' "Two surfaces by default" rule) plus the Gateway Service
Dashboard and the gateway host daemon. Source today: `agent_utilities/gateway/`
(see AGENTS.md "W5 source measurements"). Not yet populated — see AGENTS.md
"Status".
"""
