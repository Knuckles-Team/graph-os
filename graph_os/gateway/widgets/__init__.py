"""Gateway widgets — service dashboard widget implementations.

Each widget module exports a ``Widget`` class inheriting from ``BaseWidget``
that defines ``get_fields()`` and ``fetch_data()`` for a specific service.
"""
